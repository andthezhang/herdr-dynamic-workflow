[defaults]
kind = "codex"
on_blocked = "fail"

[runtime.codex]
permission = ["--sandbox", "workspace-write", "--ask-for-approval", "never"]

[runtime.codex.model]
"gpt-5.6-luna" = ["--model", "gpt-5.6-luna"]

[[machine]]
name = "local"
slots = 4
kinds = ["codex"]
